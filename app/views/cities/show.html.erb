<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @city.name %>
</p>
<p>
  <strong>Districts:</strong>
  <%=@city.districts.map { |district| district.name }.join(' ') %>
</p>

<%= link_to 'Back', cities_path %>

<%if current_user.admin?%> 
  <%= link_to "Add Districs" , :controller => "districts", :action => "new" %>
    <%= link_to "Edit Districs" , :controller => "districts", :action => "edit" %>
  <%= link_to "Delete Districs" , :controller => "districts", :action => "destroy" %>
<% end %>
